---
/* =========================================================
   LOKATIONS-KNAP
   - sm/md: auto width (content-based)
   - lg/xl: full width (card style)
   ========================================================= */

type LocationButtonSize = "sm" | "md" | "lg" | "xl";
type LocationButtonColor = "main" | "primary" | "secondary";

interface Props {
  size?: LocationButtonSize;
  color?: LocationButtonColor;
  location?: string;
  address?: string;
  stylingclasses?: string;
}

const {
  size = "md",
  color = "main",
  location = "",
  address = "",
  stylingclasses = "",
} = Astro.props as Props;

// Bruger text-size-* tokens fra tailwind.css i stedet for Tailwinds egne text-sm/base/lg.
const sizeClasses = {
  sm: "px-4 py-2 text-size-14 rounded-md",
  md: "px-5 py-3 text-size-15 rounded-md",
  lg: "px-6 py-4 text-size-17 rounded-md",
  xl: "w-full px-6 py-5 text-size-20 rounded-md",
};

const colorClasses = {
  main: "bg-(--button-main-bg) text-(--button-main-text) hover:bg-(--button-main-bg-hover)",
  primary:
    "bg-(--button-primary-bg) text-(--button-primary-text) hover:bg-(--button-primary-bg-hover)",
  secondary:
    "bg-(--button-secondary-bg) text-(--button-secondary-text) hover:bg-(--button-secondary-bg-hover)",
};

const baseButton =
  "inline-flex flex-col text-left font-sans font-semibold transition-colors duration-150 hover:scale-[1.01] active:scale-[0.99] focus:outline-none cursor-pointer";

const buttonClass = [
  baseButton,
  sizeClasses[size],
  colorClasses[color],
  stylingclasses,
]
  .filter(Boolean)
  .join(" ");
---

<button
  type="button"
  x-data="{ active: false }"
  @click="active = !active"
  class={buttonClass}
>
  <span class="font-display leading-tight">
    {location}
  </span>

  {
    (size === "lg" || size === "xl") && address && (
      <span class="text-size-14 font-sans leading-tight opacity-80">
        {address}
      </span>
    )
  }
</button>
